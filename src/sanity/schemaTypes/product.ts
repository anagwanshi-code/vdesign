/**
 * Shop/catalog fields are defined on the main `product` document
 * in `src/sanity/schemas/documents/product.ts` (ecommerce + catalog).
 */
export { product } from "../schemas/documents/product";
